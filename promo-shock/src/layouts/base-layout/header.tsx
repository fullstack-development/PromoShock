"use client";
import type { FC } from "react";

import { WalletButton } from "@promo-shock/components";

const Header: FC = () => {
  return (
    <div>
      <WalletButton />
    </div>
  );
};

export { Header };
